from dataclasses import dataclass


@dataclass(frozen=True)
class Customer:
    CustomerId: int
    FirstName: str
    LastName: str
    Country: str

    def __str__(self):
        return f"{self.FirstName} {self.LastName}"
