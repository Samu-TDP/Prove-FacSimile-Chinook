from dataclasses import dataclass


@dataclass(frozen=True)
class TrackInfo:
    TrackId: int
    Name: str
    Milliseconds: int

    def __str__(self):
        minuti = self.Milliseconds / 1000 / 60
        return f"{self.Name} ({minuti:.2f} min)"
